package tt.richTaxist;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

public class Settings4ParksAndBillingsActivity extends AppCompatActivity {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        //TODO: set on input listener instead of forcing user to press enter
        setContentView(R.layout.activity_settigs4_parks_and_billings);
    }
}
